{
    "name": "zicht/classes",
    "description": "PHP utility for conditionally joining CSS classes.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Zicht Online",
            "email": "info@zicht.nl"
        }
    ],
    "repositories": [
        {
            "type": "vcs",
            "url": "git@github.com:zicht/classes.git"
        }
    ],
    "require": {
        "php": "^7"
    },
    "require-dev": {
        "phpunit/phpunit": "^4"
    },
    "autoload": {
        "psr-4": { "": ["src/"] }
    },
    "minimum-stability": "stable",
    "scripts": {
        "test": [
            "@composer install --dev",
            "./vendor/phpunit/phpunit/phpunit"
        ]
    }
}
